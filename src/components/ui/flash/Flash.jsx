import { Box } from "@chakra-ui/react";

const Flash = () => {

    return (
        <Box 
            boxSize="full"
            bgColor="white"
            opacity="0.6"
        />
    );
};

export default Flash